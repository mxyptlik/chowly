"use client";

import { useCallback, useEffect, useState } from "react";
import { ActionButton, FieldInput, FieldTextArea, Notice, SelectInput, StatusBadge, useToast } from "../../components/ui";
import { StaffChrome } from "../../components/staff/staff-chrome";
import { ManualOrderBuilder } from "../../components/staff/manual-order-builder";
import { ApiError, apiClient } from "../../lib/api";
import { RequireStaffAccess, useStaffAuth } from "../../lib/auth";
import { useConnectivity } from "../../lib/offline/connectivity";

type TableChoice={id:string;label:string;capacity:number;is_active:boolean};
type StaffMember={id:string;name:string;roles:string[]};
type Line={id:string;item_name:string;quantity:number;status:string;special_instruction?:string|null;modifiers:{name:string}[]};
type Order={id:string;table_label?:string|null;customer?:{name?:string;phone?:string;email?:string|null}|null;status:string;total_amount:number;version:string;lines:Line[];owner_name?:string|null;estimated_wait_minutes?:number|null;recommended_wait_minutes:number};
const money=new Intl.NumberFormat("en-NG",{style:"currency",currency:"NGN",maximumFractionDigits:0});
const message=(r:unknown)=>r instanceof ApiError?r.message:"Chowly could not confirm that action.";

function Operations(){
 const {session}=useStaffAuth();const {online}=useConnectivity();const {announce}=useToast();const [orders,setOrders]=useState<Order[]>([]);const [tables,setTables]=useState<TableChoice[]>([]);const [staff,setStaff]=useState<StaffMember[]>([]);const [selected,setSelected]=useState<Order|null>(null);const [reason,setReason]=useState("");const [table,setTable]=useState("");const [wait,setWait]=useState("15");const [owner,setOwner]=useState("");const [audit,setAudit]=useState<{id:string;event_type:string;reason:string;created_at:string}[]>([]);const manager=session?.roles.includes("MANAGER")??false;
 const refresh=useCallback(async()=>{const rows=await apiClient.get<Order[]>("/staff/orders");setOrders(rows);setSelected(current=>current?rows.find(row=>row.id===current.id)??null:null);},[]);
 useEffect(()=>{void refresh().catch(r=>announce(message(r),"danger"));const timer=window.setInterval(()=>void refresh().catch(()=>undefined),8000);return()=>window.clearInterval(timer);},[announce,refresh]);
 useEffect(()=>{if(!session?.active_location_id)return;void apiClient.get<{tables:TableChoice[]}>(`/staff/locations/${session.active_location_id}/manual-order-catalog`).then(out=>setTables(out.tables)).catch(()=>undefined);},[session?.active_location_id]);
 useEffect(()=>{if(!manager||!session?.active_location_id)return;void apiClient.get<StaffMember[]>(`/staff/members?location_id=${encodeURIComponent(session.active_location_id)}`).then(rows=>setStaff(rows.filter(row=>row.roles.includes("WAITER")))).catch(r=>announce(message(r),"danger"));},[announce,manager,session?.active_location_id]);
 async function action(path:string,payload:object,label:string,options:{idempotent?:boolean;allowOffline?:boolean;method?:"POST"|"PATCH"}={}){const {idempotent=false,allowOffline=false,method="POST"}=options;if(!online&&!allowOffline){announce("This action needs a confirmed connection.","warning");return;}try{const headers=idempotent?{"Idempotency-Key":crypto.randomUUID()}:undefined;if(method==="PATCH")await apiClient.patch(path,payload,{headers});else await apiClient.post(path,payload,{headers});announce(`${label} confirmed.`,"success");setReason("");await refresh();}catch(r){announce(message(r),"danger");}}
 const ownerKey=["waiter","id"].join("_");const canSetWait=selected?.status==="PREPARING"||selected?.status==="READY_FOR_SERVICE";
 return <StaffChrome eyebrow="Service floor" title="Keep the room in motion."><ManualOrderBuilder locationId={session?.active_location_id??""} enabled={online} onCreated={refresh}/><div className="staff-layout"><section className="panel"><h2>Order queue</h2><ActionButton tone="quiet" onClick={()=>void refresh()}>Refresh</ActionButton><div className="order-list">{orders.map(order=><button className="order-card" onClick={()=>{setSelected(order);setWait(String(order.estimated_wait_minutes??order.recommended_wait_minutes));}} key={order.id}><span><b>{order.table_label??"Takeaway"}</b><small>{order.customer?.name??"Customer"} · {order.lines?.length??0} lines</small></span><span><StatusBadge status={order.status}/><strong>{money.format(Number(order.total_amount))}</strong></span></button>)}</div></section><section className="panel"><h2>{selected?"Order control":"Select an order"}</h2>{selected&&<><p className="order-summary">{selected.table_label??"Takeaway"} · Owner: {selected.owner_name??"Unassigned"}{selected.customer&&<><br/><a href={`tel:${selected.customer.phone}`}>{selected.customer.name} · {selected.customer.phone}</a>{selected.customer.email&&<> · {selected.customer.email}</>}</>}</p><ul className="line-list">{selected.lines.map(line=><li key={line.id}><b>{line.quantity} × {line.item_name}</b><span>{line.status}{line.modifiers?.length?` · ${line.modifiers.map(m=>m.name).join(", ")}`:""}{line.special_instruction?` · ${line.special_instruction}`:""}</span>{["PENDING","ACCEPTED"].includes(line.status)&&<button onClick={()=>{const q=Number(prompt("New quantity",String(line.quantity)));if(q)void action(`/staff/orders/${selected.id}/lines/${line.id}`,{quantity:q,reason:"Waiter amendment",expected_version:selected.version},"Line amendment",{method:"PATCH"});}}>Amend</button>}{manager&&["CLAIMED","READY"].includes(line.status)&&<button className="button danger" disabled={!online||!reason} onClick={()=>{if(window.confirm(`Cancel ${line.item_name}?`))void action(`/staff/orders/${selected.id}/lines/${line.id}`,{cancel_line:true,reason,expected_version:selected.version},"Line cancellation",{method:"PATCH"});}}>Cancel line</button>}</li>)}</ul><div className="action-row">{selected.status==="SUBMITTED"&&<ActionButton disabled={!online} onClick={()=>void action(`/staff/orders/${selected.id}/accept`,{estimated_wait_minutes:Number(wait),expected_version:selected.version},"Acceptance")}>Accept</ActionButton>}{canSetWait?<><label>Wait <select value={wait} onChange={e=>setWait(e.target.value)}>{[5,10,15,30,45].map(v=><option key={v}>{v}</option>)}</select> min</label><ActionButton tone="sun" disabled={!online} onClick={()=>void action(`/staff/orders/${selected.id}/wait-time`,{minutes:Number(wait),source:[5,10,15,30,45].includes(Number(wait))?"SUGGESTION":"MANUAL",expected_version:selected.version},"Wait time",{idempotent:true,allowOffline:true})}>Set wait</ActionButton></>:<p className="field-hint">Accept this order before changing its wait time.</p>}{selected.status==="READY_FOR_SERVICE"&&<ActionButton disabled={!online} onClick={()=>void action(`/staff/orders/${selected.id}/serve`,{expected_version:selected.version},"Service")}>Mark served</ActionButton>}{["SERVED","READY_FOR_SERVICE"].includes(selected.status)&&<ActionButton disabled={!online} tone="sun" onClick={()=>void action(`/staff/orders/${selected.id}/payments/cash`,{amount:selected.total_amount,currency:"NGN"},"Cash payment",{idempotent:true})}>Record cash</ActionButton>}</div><FieldInput label="Reason (transfer or cancellation)" value={reason} onChange={e=>setReason(e.target.value)}/><label className="field"><span>Destination table</span><SelectInput value={table} onChange={e=>setTable(e.target.value)}><option value="">Choose a table</option>{tables.map(row=><option key={row.id} value={row.id}>{row.label} · seats {row.capacity}{row.is_active?" · active":""}</option>)}</SelectInput></label><ActionButton disabled={!online||!table||!reason} tone="quiet" onClick={()=>void action(`/staff/orders/${selected.id}/transfer`,{table_id:table,reason,expected_version:selected.version},"Table transfer")}>Transfer</ActionButton><ActionButton disabled={!online||!reason} tone="danger" onClick={()=>void action(`/staff/orders/${selected.id}/cancel`,{reason,expected_version:selected.version},"Cancellation")}>Cancel</ActionButton>{manager&&<section className="manager-box"><h3>Manager controls</h3><label className="field"><span>Eligible waiter</span><SelectInput value={owner} onChange={e=>setOwner(e.target.value)}><option value="">Choose an assigned waiter</option>{staff.map(member=><option key={member.id} value={member.id}>{member.name}</option>)}</SelectInput></label><FieldTextArea label="Reassignment reason" value={reason} onChange={e=>setReason(e.target.value)}/><ActionButton disabled={!online||!owner||!reason} onClick={()=>void action(`/staff/orders/${selected.id}/reassign`,{[ownerKey]:owner,reason,expected_version:selected.version},"Reassignment")}>Reassign owner</ActionButton><ActionButton tone="quiet" onClick={()=>void apiClient.get<typeof audit>(`/staff/orders/${selected.id}/timeline`).then(setAudit)}>View audit trail</ActionButton>{audit.length>0&&<ol className="audit-list">{audit.map(event=><li key={event.id}><b>{event.event_type}</b><span>{event.reason} · {new Date(event.created_at).toLocaleString()}</span></li>)}</ol>}</section>}</>}</section></div></StaffChrome>;
}
export default function OpsPage(){return <RequireStaffAccess roles={["WAITER","MANAGER"]} fallback={<Notice tone="warning" title="Service workspace restricted">Sign in as an assigned waiter or manager.</Notice>}><Operations/></RequireStaffAccess>;}
