import { RestaurantDirectory } from "../../components/public/public-restaurant";

export const metadata = { title: "Find a restaurant — Chowly" };

export default function RestaurantsPage() { return <RestaurantDirectory />; }
